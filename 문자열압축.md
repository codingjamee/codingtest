### 문재

반복되는 알파벳 앞에 반복되는 숫자 작성 (ex : 숫자+알파벳)
단위를 2개 3개 등등으로 쪼갤 수 있으며 그것의 길이가 가장 짧은 길이를 return하는 함수
가장 앞부터 정해진 만큼씩 잘라야한다

### 입출력 예

1. 입력 : abcabcabcabc6de

for문 반복
stack을 만든다
pointer를 만든다
split을 사용한다

1. start = 0, end = 1, stack에 0~1까지 추가
   count = 1
   substring(0, 1) (a)
   substring(1, 2) (b)
   이전 string과 겹치면 숫자 + 알파벳
   아니면 그냥 알파벳

2. start = 0, end = 2,
   마지막 배열 요소를 본다
   substring(0, 2) (ab)
   substring(0, 2) (ab)

- 반복하는 길이는 Math.floor(string.length / 2)
  처음부터 stack에 0~ 반복최대 길이까지 잘라서 넣는다?
  a, aa, aab, aabb
  a스택에 넣고
  그다음 stack의 글자 a가 현재와 동일하므로 2a로

3. 1~length/2만큼 자르는걸 반보하고
   그 내에서 다음 문자열과 비교하여 반복되면 1을 증가 반복되지 않으면 count를 함께 return

### 풀어보기

문자열을 각각 1부터 string.length/2 까지 반복

```js
const arr = [];

for (let unit =1; unit <= Math.floor(s.length/2)); unit ++) {
  let countedString = ''; //반복 숫자와 문자열
  let count = 1;
  let prev = s.substring(0, unit) //처음 문자열
  for (let i = unit; i < s.length-1; i+=unit) {
    let current = s.substring(i, i + unit) //현재 보고있는 문자열

    if(prev === current) {
      count ++;
    } else {
      countedString = count === 1 ? current : count + current
      prev = current;
      count = 1;
    }

  }
  const remainder = s.length % unit;
  if(remainder > 0) {
    countedString += s.slice(s.length - remainder) //남은 문자열
  }
  arr.push(countedString.length) //가장 짧은 길이만 찾는다. 어떤 문자열인지 필요 없음 
}
return Math.min(...arr)


```
